name: intel_ska
channels:
  - anaconda
  - conda-forge
  - bioconda
  - defaults
  - r
dependencies:
    - python
    - pip
    - numpy
    - scipy
    - docopt
    - cmake >= 3.12
    - pybind11
    - zlib
    - biopython
    # osx
    - llvm-openmp  # [osx]
    - gfortran_impl_osx-64  # [osx]
